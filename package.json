{
  "name": "lpt",
  "version": "0.0.0",
  "description": "last month photo from my tumblr",
  "main": "index.js",
  "scripts": {
    "start": "npm run fetch && npm run download && npm run modify",
    "fetch": "lsc fetch.ls",
    "download": "rm -rf images && mkdir images && lsc download.ls",
    "modify": "rm -rf converted-images && mkdir converted-images && lsc modify.ls"
  },
  "author": "jigsaw (http://jgs.me)",
  "license": "MIT",
  "dependencies": {
    "livescript": "^1.5.0",
    "lson": "^1.0.3",
    "request": "^2.75.0",
    "tumblr.js": "1.1.1"
  }
}
